
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Solana Keene's 1st Birthday & Christening</title>
    <!-- Tailwind CSS via CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Alex+Brush&family=Cormorant+Garamond:ital,wght@0,400;0,600;1,400&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        pastel: {
                            50: '#FFF5F7',
                            100: '#FFEBEF',
                            200: '#FFCCD5',
                            300: '#FFB3C1',
                            400: '#FF85A1',
                            500: '#F76C82',
                            gold: '#D4AF37',
                            goldlight: '#F3E5AB',
                            rose: '#A44265',
                            dark: '#4A3B40'
                        }
                    },
                    fontFamily: {
                        cursive: ['"Alex Brush"', 'cursive'],
                        serif: ['"Cormorant Garamond"', 'serif'],
                        sans: ['"Montserrat"', 'sans-serif'],
                    }
                }
            }
        }
    </script>
    <style>
        /* Floating background petals effect */
        .petal {
            position: absolute;
            background: linear-gradient(135deg, #FFCCD5, #FFEBEF);
            border-radius: 150% 0 150% 0;
            opacity: 0.6;
            pointer-events: none;
            animation: float 12s infinite linear;
        }

        @keyframes float {
            0% {
                transform: translateY(105vh) rotate(0deg);
                opacity: 0.7;
            }
            100% {
                transform: translateY(-10vh) rotate(360deg);
                opacity: 0;
            }
        }

        /* Subtle glow effects */
        .gold-border-glow {
            box-shadow: 0 0 15px rgba(212, 175, 55, 0.25);
        }

        /* Glassmorphism styling */
        .glass-card {
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 204, 213, 0.5);
        }

        /* Smooth Scrolling */
        html {
            scroll-behavior: smooth;
        }
    </style>
</head>
<body class="bg-pastel-50 text-pastel-dark font-sans min-h-screen relative overflow-x-hidden">

    <!-- Floating Background Petals -->
    <div id="petals-container" class="fixed inset-0 pointer-events-none z-0 overflow-hidden"></div>

    <!-- Navigation Bar -->
    <nav class="fixed top-0 left-0 right-0 z-50 glass-card px-4 py-3 shadow-sm border-b border-pastel-200/50">
        <div class="max-w-4xl mx-auto flex justify-between items-center">
            <a href="#hero" class="font-cursive text-2xl md:text-3xl text-pastel-rose font-bold">Solana Keene</a>
            <div class="hidden sm:flex space-x-6 text-sm font-medium text-pastel-dark">
                <a href="#details" class="hover:text-pastel-500 transition-colors">Details</a>
                <a href="#timeline" class="hover:text-pastel-500 transition-colors">Schedule</a>
                <a href="#rsvp" class="hover:text-pastel-500 transition-colors">RSVP</a>
                <a href="#wishes" class="hover:text-pastel-500 transition-colors">Wishes</a>
            </div>
            <a href="#rsvp" class="sm:hidden px-3 py-1.5 bg-pastel-300 text-white rounded-full text-xs font-semibold shadow hover:bg-pastel-400 transition-colors">
                RSVP Now
            </a>
        </div>
    </nav>

    <!-- Hero Section -->
    <section id="hero" class="relative pt-28 pb-16 md:pt-36 md:pb-24 flex flex-col items-center justify-center text-center px-4 z-10 min-h-screen">
        <div class="max-w-3xl mx-auto space-y-6">
            <!-- Decorative Arch / Frame -->
            <div class="inline-block p-1 bg-gradient-to-b from-pastel-goldlight via-pastel-200 to-pastel-goldlight rounded-t-full shadow-lg">
                <div class="bg-white/90 rounded-t-full px-8 py-12 md:px-16 md:py-16 backdrop-blur-sm border border-pastel-100 flex flex-col items-center">
                    
                    <div class="flex items-center space-x-2 text-pastel-gold font-serif text-sm md:text-base tracking-widest uppercase mb-2">
                        <i data-lucide="sparkles" class="w-4 h-4"></i>
                        <span>You Are Cordially Invited</span>
                        <i data-lucide="sparkles" class="w-4 h-4"></i>
                    </div>

                    <h2 class="text-xs md:text-sm font-semibold tracking-widest text-pastel-rose uppercase mt-2 mb-1">To Celebrate The First Birthday & Holy Baptism Of</h2>
                    
                    <!-- Main Name -->
                    <h1 class="font-cursive text-5xl sm:text-7xl md:text-8xl text-pastel-rose my-2 tracking-wide leading-tight drop-shadow-sm">
                        Solana Keene
                    </h1>
                    <p class="font-serif text-2xl md:text-3xl text-pastel-dark italic font-semibold tracking-wider mb-6">
                        M. Carlos
                    </p>

                    <!-- Divider -->
                    <div class="flex items-center justify-center space-x-3 w-full my-4">
                        <div class="h-px bg-pastel-gold/40 flex-1"></div>
                        <i data-lucide="cross" class="w-5 h-5 text-pastel-gold"></i>
                        <i data-lucide="heart" class="w-4 h-4 text-pastel-400 fill-pastel-200"></i>
                        <i data-lucide="cross" class="w-5 h-5 text-pastel-gold"></i>
                        <div class="h-px bg-pastel-gold/40 flex-1"></div>
                    </div>

                    <p class="font-serif text-lg md:text-xl text-gray-700 max-w-md italic mt-2">
                        "Every good and perfect gift is from above, coming down from the Father of the heavenly lights." 
                        <span class="block text-xs font-sans not-italic text-pastel-rose/80 mt-1">— James 1:17</span>
                    </p>

                    <!-- Date Badge -->
                    <div class="mt-8 bg-pastel-100/80 border border-pastel-200 rounded-2xl px-6 py-4 shadow-inner">
                        <p class="text-xs uppercase tracking-widest text-pastel-rose font-semibold">Save The Date</p>
                        <p class="font-serif text-2xl md:text-3xl font-bold text-pastel-dark tracking-wide mt-1">
                            Wednesday, November 11, 2026
                        </p>
                        <p class="text-sm text-pastel-dark/80 mt-1 flex items-center justify-center gap-1">
                            <i data-lucide="map-pin" class="w-4 h-4 text-pastel-400"></i> Festival Mall, Alabang
                        </p>
                    </div>

                </div>
            </div>

            <!-- Countdown Timer -->
            <div class="pt-6">
                <h3 class="text-xs md:text-sm font-semibold uppercase tracking-widest text-pastel-rose mb-4">Counting Down To The Big Day</h3>
                <div class="grid grid-cols-4 gap-2 md:gap-4 max-w-md mx-auto">
                    <div class="glass-card p-3 rounded-2xl text-center shadow-sm border border-pastel-200">
                        <span id="cd-days" class="font-serif text-2xl md:text-4xl font-bold text-pastel-rose">00</span>
                        <span class="block text-[10px] md:text-xs text-pastel-dark uppercase tracking-wider font-medium mt-1">Days</span>
                    </div>
                    <div class="glass-card p-3 rounded-2xl text-center shadow-sm border border-pastel-200">
                        <span id="cd-hours" class="font-serif text-2xl md:text-4xl font-bold text-pastel-rose">00</span>
                        <span class="block text-[10px] md:text-xs text-pastel-dark uppercase tracking-wider font-medium mt-1">Hours</span>
                    </div>
                    <div class="glass-card p-3 rounded-2xl text-center shadow-sm border border-pastel-200">
                        <span id="cd-minutes" class="font-serif text-2xl md:text-4xl font-bold text-pastel-rose">00</span>
                        <span class="block text-[10px] md:text-xs text-pastel-dark uppercase tracking-wider font-medium mt-1">Mins</span>
                    </div>
                    <div class="glass-card p-3 rounded-2xl text-center shadow-sm border border-pastel-200">
                        <span id="cd-seconds" class="font-serif text-2xl md:text-4xl font-bold text-pastel-rose">00</span>
                        <span class="block text-[10px] md:text-xs text-pastel-dark uppercase tracking-wider font-medium mt-1">Secs</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Event Details Cards -->
    <section id="details" class="py-16 px-4 max-w-5xl mx-auto relative z-10">
        <div class="text-center mb-12">
            <h2 class="font-cursive text-4xl md:text-5xl text-pastel-rose">Celebration Details</h2>
            <p class="text-sm md:text-base text-gray-600 font-serif italic mt-1">Join us in celebrating two special milestones in one wonderful day</p>
            <div class="w-16 h-0.5 bg-pastel-300 mx-auto mt-3 rounded-full"></div>
        </div>

        <div class="grid md:grid-cols-2 gap-8">
            <!-- Event 1: Holy Baptism -->
            <div class="glass-card rounded-3xl p-6 md:p-8 shadow-md border border-pastel-200 flex flex-col justify-between hover:shadow-lg transition-shadow">
                <div>
                    <div class="w-12 h-12 bg-pastel-100 rounded-2xl flex items-center justify-center text-pastel-rose mb-4 border border-pastel-200">
                        <i data-lucide="church" class="w-6 h-6"></i>
                    </div>
                    <span class="text-xs font-semibold tracking-widest text-pastel-gold uppercase bg-pastel-50 px-3 py-1 rounded-full border border-pastel-gold/20 inline-block mb-3">Part 1</span>
                    <h3 class="font-serif text-2xl md:text-3xl font-bold text-pastel-dark mb-2">The Holy Baptism</h3>
                    <p class="text-sm text-gray-600 mb-6">Where Solana receives her blessing of faith, surrounded by her family, godparents, and loved ones.</p>

                    <div class="space-y-3 text-sm text-gray-700 border-t border-pastel-100 pt-4">
                        <div class="flex items-start gap-3">
                            <i data-lucide="calendar" class="w-5 h-5 text-pastel-400 shrink-0 mt-0.5"></i>
                            <div>
                                <p class="font-semibold text-pastel-dark">Date & Time</p>
                                <p>Wednesday, Nov 11, 2026 • 10:00 AM</p>
                            </div>
                        </div>
                        <div class="flex items-start gap-3">
                            <i data-lucide="map-pin" class="w-5 h-5 text-pastel-400 shrink-0 mt-0.5"></i>
                            <div>
                                <p class="font-semibold text-pastel-dark">Location</p>
                                <p>Festival Mall Ceremony Hall / Chapel</p>
                                <p class="text-xs text-gray-500">Festival Mall, Alabang, Muntinlupa City</p>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="mt-6 pt-4 border-t border-pastel-100 flex justify-between items-center text-xs text-pastel-rose font-medium">
                    <span>Dress Code: Soft Pastels / White</span>
                    <i data-lucide="heart" class="w-4 h-4 fill-pastel-200"></i>
                </div>
            </div>

            <!-- Event 2: 1st Birthday Party -->
            <div class="glass-card rounded-3xl p-6 md:p-8 shadow-md border border-pastel-200 flex flex-col justify-between hover:shadow-lg transition-shadow">
                <div>
                    <div class="w-12 h-12 bg-pastel-100 rounded-2xl flex items-center justify-center text-pastel-rose mb-4 border border-pastel-200">
                        <i data-lucide="cake" class="w-6 h-6"></i>
                    </div>
                    <span class="text-xs font-semibold tracking-widest text-pastel-gold uppercase bg-pastel-50 px-3 py-1 rounded-full border border-pastel-gold/20 inline-block mb-3">Part 2</span>
                    <h3 class="font-serif text-2xl md:text-3xl font-bold text-pastel-dark mb-2">1st Birthday Party</h3>
                    <p class="text-sm text-gray-600 mb-6">A festive reception filled with games, sweet treats, music, and joyful memories to celebrate her first year.</p>

                    <div class="space-y-3 text-sm text-gray-700 border-t border-pastel-100 pt-4">
                        <div class="flex items-start gap-3">
                            <i data-lucide="clock" class="w-5 h-5 text-pastel-400 shrink-0 mt-0.5"></i>
                            <div>
                                <p class="font-semibold text-pastel-dark">Reception Time</p>
                                <p>Wednesday, Nov 11, 2026 • 12:00 PM</p>
                            </div>
                        </div>
                        <div class="flex items-start gap-3">
                            <i data-lucide="map-pin" class="w-5 h-5 text-pastel-400 shrink-0 mt-0.5"></i>
                            <div>
                                <p class="font-semibold text-pastel-dark">Venue</p>
                                <p>Events Hall, Festival Mall Alabang</p>
                                <p class="text-xs text-gray-500">Corporate Ave, Filinvest City, Alabang, Muntinlupa</p>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="mt-6 pt-4 border-t border-pastel-100 flex justify-between items-center text-xs text-pastel-rose font-medium">
                    <span>Theme: Soft Pastel Pink Wonderland</span>
                    <i data-lucide="sparkles" class="w-4 h-4"></i>
                </div>
            </div>
        </div>
    </section>

    <!-- Map & Location Section -->
    <section class="py-12 px-4 max-w-5xl mx-auto relative z-10">
        <div class="glass-card rounded-3xl p-6 md:p-8 shadow-md border border-pastel-200 text-center">
            <h3 class="font-serif text-2xl md:text-3xl font-bold text-pastel-dark mb-2">Venue Location</h3>
            <p class="text-sm text-gray-600 mb-6">Festival Mall Alabang • Filinvest City, Muntinlupa</p>
            
            <!-- Map Embed Placeholder / Container -->
            <div class="w-full h-64 md:h-80 bg-pastel-100 rounded-2xl overflow-hidden shadow-inner relative border border-pastel-200">
                <iframe 
                    title="Festival Mall Alabang Location"
                    src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3864.8383822187655!2d121.0379201!3d14.4178553!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x3397d025e9821267%3A0x6b4fb669f91a6d71!2sFestival%20Mall!5e0!3m2!1sen!2sph!4v1700000000000!5m2!1sen!2sph" 
                    class="w-full h-full border-0" 
                    allowfullscreen="" 
                    loading="lazy">
                </iframe>
            </div>

            <div class="mt-6 flex flex-wrap justify-center gap-4 text-xs md:text-sm">
                <a href="https://maps.google.com/?q=Festival+Mall+Alabang" target="_blank" class="inline-flex items-center gap-2 px-5 py-2.5 bg-pastel-300 text-white rounded-full hover:bg-pastel-400 transition-colors shadow">
                    <i data-lucide="navigation" class="w-4 h-4"></i> Open in Google Maps
                </a>
            </div>
        </div>
    </section>

    <!-- Program / Schedule Section -->
    <section id="timeline" class="py-16 px-4 max-w-3xl mx-auto relative z-10">
        <div class="text-center mb-12">
            <h2 class="font-cursive text-4xl md:text-5xl text-pastel-rose">Event Schedule</h2>
            <p class="text-sm md:text-base text-gray-600 font-serif italic">What to expect on Solana's special day</p>
            <div class="w-16 h-0.5 bg-pastel-300 mx-auto mt-3 rounded-full"></div>
        </div>

        <div class="relative border-l-2 border-pastel-300 ml-6 md:ml-32 space-y-8">
            <!-- Item 1 -->
            <div class="relative pl-6 md:pl-8">
                <span class="absolute -left-3 top-1 w-6 h-6 bg-pastel-100 border-2 border-pastel-400 rounded-full flex items-center justify-center text-pastel-rose">
                    <i data-lucide="cross" class="w-3 h-3"></i>
                </span>
                <span class="hidden md:block absolute -left-32 top-1 text-xs font-semibold text-pastel-rose w-24 text-right">10:00 AM</span>
                <span class="md:hidden text-xs font-bold text-pastel-rose block mb-1">10:00 AM</span>
                <h4 class="font-serif text-lg font-bold text-pastel-dark">Christening Rites</h4>
                <p class="text-xs text-gray-600">Sacrament of Holy Baptism with godparents & family.</p>
            </div>

            <!-- Item 2 -->
            <div class="relative pl-6 md:pl-8">
                <span class="absolute -left-3 top-1 w-6 h-6 bg-pastel-100 border-2 border-pastel-400 rounded-full flex items-center justify-center text-pastel-rose">
                    <i data-lucide="camera" class="w-3 h-3"></i>
                </span>
                <span class="hidden md:block absolute -left-32 top-1 text-xs font-semibold text-pastel-rose w-24 text-right">11:30 AM</span>
                <span class="md:hidden text-xs font-bold text-pastel-rose block mb-1">11:30 AM</span>
                <h4 class="font-serif text-lg font-bold text-pastel-dark">Photo Session & Transfer</h4>
                <p class="text-xs text-gray-600">Capturing memories before transferring to the reception venue.</p>
            </div>

            <!-- Item 3 -->
            <div class="relative pl-6 md:pl-8">
                <span class="absolute -left-3 top-1 w-6 h-6 bg-pastel-100 border-2 border-pastel-400 rounded-full flex items-center justify-center text-pastel-rose">
                    <i data-lucide="utensils" class="w-3 h-3"></i>
                </span>
                <span class="hidden md:block absolute -left-32 top-1 text-xs font-semibold text-pastel-rose w-24 text-right">12:00 PM</span>
                <span class="md:hidden text-xs font-bold text-pastel-rose block mb-1">12:00 PM</span>
                <h4 class="font-serif text-lg font-bold text-pastel-dark">Grand Entrance & Lunch</h4>
                <p class="text-xs text-gray-600">Welcoming Solana Keene and enjoying a delicious feast together.</p>
            </div>

            <!-- Item 4 -->
            <div class="relative pl-6 md:pl-8">
                <span class="absolute -left-3 top-1 w-6 h-6 bg-pastel-100 border-2 border-pastel-400 rounded-full flex items-center justify-center text-pastel-rose">
                    <i data-lucide="gift" class="w-3 h-3"></i>
                </span>
                <span class="hidden md:block absolute -left-32 top-1 text-xs font-semibold text-pastel-rose w-24 text-right">01:30 PM</span>
                <span class="md:hidden text-xs font-bold text-pastel-rose block mb-1">01:30 PM</span>
                <h4 class="font-serif text-lg font-bold text-pastel-dark">Games, Program & Cake Cutting</h4>
                <p class="text-xs text-gray-600">Fun activities, blowing of candles, and special messages.</p>
            </div>
        </div>
    </section>

    <!-- Dress Code & Gift Note -->
    <section class="py-12 px-4 max-w-4xl mx-auto relative z-10">
        <div class="grid md:grid-cols-2 gap-6">
            <!-- Dress Code Card -->
            <div class="glass-card rounded-3xl p-6 border border-pastel-200 text-center">
                <div class="w-10 h-10 bg-pastel-100 rounded-full flex items-center justify-center mx-auto mb-3 text-pastel-rose">
                    <i data-lucide="shirt" class="w-5 h-5"></i>
                </div>
                <h3 class="font-serif text-xl font-bold text-pastel-dark mb-2">Attire Guide</h3>
                <p class="text-xs text-gray-600 mb-4">We would love to see you in soft pastel shades to complement Solana's special theme.</p>
                <div class="flex justify-center items-center gap-2">
                    <span class="w-6 h-6 rounded-full bg-[#FFEBEF] border border-gray-200" title="Soft Pink"></span>
                    <span class="w-6 h-6 rounded-full bg-[#FFCCD5] border border-gray-200" title="Pastel Pink"></span>
                    <span class="w-6 h-6 rounded-full bg-[#FFF9E6] border border-gray-200" title="Cream"></span>
                    <span class="w-6 h-6 rounded-full bg-[#FFFFFF] border border-gray-200" title="White"></span>
                    <span class="w-6 h-6 rounded-full bg-[#E2F0D9] border border-gray-200" title="Pastel Mint"></span>
                </div>
            </div>

            <!-- Gift Note Card -->
            <div class="glass-card rounded-3xl p-6 border border-pastel-200 text-center">
                <div class="w-10 h-10 bg-pastel-100 rounded-full flex items-center justify-center mx-auto mb-3 text-pastel-rose">
                    <i data-lucide="gift" class="w-5 h-5"></i>
                </div>
                <h3 class="font-serif text-xl font-bold text-pastel-dark mb-2">Gift Guide</h3>
                <p class="text-xs text-gray-600 leading-relaxed">
                    Your presence at our celebration is the greatest gift of all! However, if you wish to honor Solana with a gift, monetary blessings for her future savings or educational fund are deeply appreciated.
                </p>
            </div>
        </div>
    </section>

    <!-- RSVP Form Section Persisted in Memory -->
    <section id="rsvp" class="py-16 px-4 max-w-2xl mx-auto relative z-10">
        <div class="glass-card rounded-3xl p-8 md:p-10 shadow-xl border-2 border-pastel-200 relative overflow-hidden">
            <div class="absolute -top-12 -right-12 w-32 h-32 bg-pastel-200/50 rounded-full blur-2xl"></div>
            
            <div class="text-center mb-8">
                <span class="text-xs font-semibold tracking-widest text-pastel-rose uppercase">Kindly Respond By</span>
                <h2 class="font-cursive text-4xl md:text-5xl text-pastel-rose mt-1">RSVP</h2>
                <p class="text-xs text-gray-500 mt-1">Please confirm your attendance on or before October 15, 2026</p>
            </div>

            <form id="rsvpForm" onsubmit="handleRSVP(event)" class="space-y-5">
                <div>
                    <label class="block text-xs font-semibold text-pastel-dark uppercase tracking-wider mb-2" for="guestName">Your Name(s)</label>
                    <input type="text" id="guestName" required placeholder="e.g. Maria & John Santos" 
                           class="w-full px-4 py-3 rounded-xl border border-pastel-200 focus:outline-none focus:ring-2 focus:ring-pastel-400 bg-white/80 text-sm">
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                        <label class="block text-xs font-semibold text-pastel-dark uppercase tracking-wider mb-2" for="attendance">Will You Attend?</label>
                        <select id="attendance" required class="w-full px-4 py-3 rounded-xl border border-pastel-200 focus:outline-none focus:ring-2 focus:ring-pastel-400 bg-white/80 text-sm">
                            <option value="">Select Response</option>
                            <option value="Joyfully Accepts">Joyfully Accepts</option>
                            <option value="Regretfully Declines">Regretfully Declines</option>
                        </select>
                    </div>

                    <div>
                        <label class="block text-xs font-semibold text-pastel-dark uppercase tracking-wider mb-2" for="guestCount">Number of Attendees</label>
                        <select id="guestCount" class="w-full px-4 py-3 rounded-xl border border-pastel-200 focus:outline-none focus:ring-2 focus:ring-pastel-400 bg-white/80 text-sm">
                            <option value="1">1 Person</option>
                            <option value="2">2 Persons</option>
                            <option value="3">3 Persons</option>
                            <option value="4">4+ Persons</option>
                        </select>
                    </div>
                </div>

                <div>
                    <label class="block text-xs font-semibold text-pastel-dark uppercase tracking-wider mb-2" for="guestMessage">Message for Solana Keene</label>
                    <textarea id="guestMessage" rows="3" placeholder="Write a sweet birthday wish or blessing..." 
                              class="w-full px-4 py-3 rounded-xl border border-pastel-200 focus:outline-none focus:ring-2 focus:ring-pastel-400 bg-white/80 text-sm resize-none"></textarea>
                </div>

                <button type="submit" class="w-full py-3.5 px-6 bg-pastel-400 hover:bg-pastel-500 text-white font-semibold rounded-xl shadow-md transition-all transform active:scale-95 text-sm flex items-center justify-center gap-2">
                    <i data-lucide="send" class="w-4 h-4"></i> Submit Response
                </button>
            </form>

            <div id="rsvpSuccess" class="hidden text-center py-8 space-y-3">
                <div class="w-16 h-16 bg-pastel-100 text-pastel-rose rounded-full flex items-center justify-center mx-auto border border-pastel-300">
                    <i data-lucide="heart-handshake" class="w-8 h-8"></i>
                </div>
                <h3 class="font-serif text-2xl font-bold text-pastel-dark">Response Received!</h3>
                <p class="text-sm text-gray-600 max-w-sm mx-auto">Thank you so much! Your response has been saved. We look forward to celebrating with you.</p>
                <button onclick="resetRSVPForm()" class="mt-4 text-xs text-pastel-rose underline hover:text-pastel-500">Send another response</button>
            </div>
        </div>
    </section>

    <!-- Guestbook / Wish Board Section -->
    <section id="wishes" class="py-16 px-4 max-w-4xl mx-auto relative z-10">
        <div class="text-center mb-10">
            <h2 class="font-cursive text-4xl md:text-5xl text-pastel-rose">Wishes for Baby Solana</h2>
            <p class="text-sm md:text-base text-gray-600 font-serif italic mt-1">Warm blessings and messages from family & friends</p>
            <div class="w-16 h-0.5 bg-pastel-300 mx-auto mt-3 rounded-full"></div>
        </div>

        <div id="wishesContainer" class="grid sm:grid-cols-2 gap-4">
            <!-- Dynamic Wishes will append here -->
            <div class="glass-card p-5 rounded-2xl border border-pastel-200 shadow-sm">
                <div class="flex justify-between items-start mb-2">
                    <h4 class="font-serif font-bold text-pastel-dark">Ninang Sarah</h4>
                    <span class="text-[10px] text-pastel-rose font-medium bg-pastel-100 px-2 py-0.5 rounded-full">Godmother</span>
                </div>
                <p class="text-xs text-gray-600 italic">"Happy 1st Birthday & Christening, baby Solana! May God shower you with endless happiness, health, and love!"</p>
            </div>

            <div class="glass-card p-5 rounded-2xl border border-pastel-200 shadow-sm">
                <div class="flex justify-between items-start mb-2">
                    <h4 class="font-serif font-bold text-pastel-dark">Tito Marco & Tita Grace</h4>
                    <span class="text-[10px] text-pastel-rose font-medium bg-pastel-100 px-2 py-0.5 rounded-full">Family</span>
                </div>
                <p class="text-xs text-gray-600 italic">"Welcome to the Christian world, little princess! Can't wait to see you grow up into a sweet, wonderful girl."</p>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="py-10 text-center border-t border-pastel-200/60 bg-white/50 relative z-10">
        <h3 class="font-cursive text-3xl text-pastel-rose">Solana Keene M. Carlos</h3>
        <p class="text-xs text-gray-500 mt-1">November 11, 2026 • Festival Mall, Alabang</p>
        <p class="text-[11px] text-pastel-rose/70 mt-4">Made with ♥ for Solana's Big Day</p>
    </footer>

    <!-- JavaScript Interactions -->
    <script>
        // Initialize Lucide Icons
        lucide.createIcons();

        // 1. Floating Petals Animation
        const petalsContainer = document.getElementById('petals-container');
        const petalCount = 15;

        for (let i = 0; i < petalCount; i++) {
            const petal = document.createElement('div');
            petal.className = 'petal';
            const size = Math.random() * 15 + 10; // 10px to 25px
            petal.style.width = `${size}px`;
            petal.style.height = `${size}px`;
            petal.style.left = `${Math.random() * 100}vw`;
            petal.style.animationDuration = `${Math.random() * 6 + 8}s`; // 8s to 14s
            petal.style.animationDelay = `${Math.random() * 5}s`;
            petalsContainer.appendChild(petal);
        }

        // 2. Countdown Timer Target: November 11, 2026
        const targetDate = new Date('November 11, 2026 10:00:00').getTime();

        function updateCountdown() {
            const now = new Date().getTime();
            const difference = targetDate - now;

            if (difference > 0) {
                const days = Math.floor(difference / (1000 * 60 * 60 * 24));
                const hours = Math.floor((difference % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
                const minutes = Math.floor((difference % (1000 * 60 * 60)) / (1000 * 60));
                const seconds = Math.floor((difference % (1000 * 60)) / 1000);

                document.getElementById('cd-days').innerText = days < 10 ? '0' + days : days;
                document.getElementById('cd-hours').innerText = hours < 10 ? '0' + hours : hours;
                document.getElementById('cd-minutes').innerText = minutes < 10 ? '0' + minutes : minutes;
                document.getElementById('cd-seconds').innerText = seconds < 10 ? '0' + seconds : seconds;
            } else {
                document.getElementById('cd-days').innerText = '00';
                document.getElementById('cd-hours').innerText = '00';
                document.getElementById('cd-minutes').innerText = '00';
                document.getElementById('cd-seconds').innerText = '00';
            }
        }
        setInterval(updateCountdown, 1000);
        updateCountdown();

        // 3. In-Memory RSVP Handling & Wish Board Update
        function handleRSVP(e) {
            e.preventDefault();
            const name = document.getElementById('guestName').value;
            const attendance = document.getElementById('attendance').value;
            const message = document.getElementById('guestMessage').value;

            // Show success state
            document.getElementById('rsvpForm').classList.add('hidden');
            document.getElementById('rsvpSuccess').classList.remove('hidden');

            // If guest left a message, add it to wishes section live
            if (message && message.trim() !== "") {
                const wishesContainer = document.getElementById('wishesContainer');
                const wishCard = document.createElement('div');
                wishCard.className = 'glass-card p-5 rounded-2xl border border-pastel-200 shadow-sm transition-all duration-500 transform translate-y-2';
                wishCard.innerHTML = `
                    <div class="flex justify-between items-start mb-2">
                        <h4 class="font-serif font-bold text-pastel-dark">${escapeHtml(name)}</h4>
                        <span class="text-[10px] text-pastel-rose font-medium bg-pastel-100 px-2 py-0.5 rounded-full">${attendance === 'Joyfully Accepts' ? 'Attending' : 'Guest'}</span>
                    </div>
                    <p class="text-xs text-gray-600 italic">"${escapeHtml(message)}"</p>
                `;
                wishesContainer.prepend(wishCard);
            }
        }

        function resetRSVPForm() {
            document.getElementById('rsvpForm').reset();
            document.getElementById('rsvpForm').classList.remove('hidden');
            document.getElementById('rsvpSuccess').classList.add('hidden');
        }

        function escapeHtml(text) {
            return text.replace(/[&<>"']/g, function(m) {
                return {
                    '&': '&amp;',
                    '<': '&lt;',
                    '>': '&gt;',
                    '"': '&quot;',
                    "'": '&#039;'
                }[m];
            });
        }
    </script>
</body>
</html>

```
        header {
            min-height: 90vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 2rem;
            background: linear-gradient(180deg, #FFFFFF 0%, var(--pastel-pink-light) 100%);
            position: relative;
        }

        .subtitle {
            font-size: 0.95rem;
            letter-spacing: 3px;
            text-transform: uppercase;
            color: #8C7A7E;
            margin-bottom: 1rem;
        }

        h1.name {
            font-family: 'Great Vibes', cursive;
            font-size: 4rem;
            color: var(--pastel-rose);
            margin-bottom: 0.5rem;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.05);
        }

        .event-tag {
            font-size: 1.2rem;
            font-weight: 300;
            margin-bottom: 2rem;
        }

        /* --- Countdown Timer --- */
        .countdown-container {
            display: flex;
            gap: 1rem;
            margin-top: 1.5rem;
        }

        .time-box {
            background: var(--white);
            padding: 1rem;
            border-radius: 12px;
            min-width: 75px;
            box-shadow: var(--shadow);
            border: 1px solid var(--pastel-pink-main);
        }

        .time-box span {
            display: block;
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--pastel-rose);
        }

        .time-box label {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* --- Content Sections --- */
        section {
            max-width: 650px;
            margin: 0 auto;
            padding: 3rem 1.5rem;
        }

        .card {
            background: var(--white);
            padding: 2.5rem 1.5rem;
            border-radius: 20px;
            box-shadow: var(--shadow);
            border: 2px solid #FFF;
            margin-bottom: 2rem;
        }

        h2 {
            font-family: 'Great Vibes', cursive;
            font-size: 2.5rem;
            color: var(--pastel-rose);
            margin-bottom: 1rem;
        }

        .details-item {
            margin: 1.5rem 0;
        }

        .details-item strong {
            display: block;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: #8C7A7E;
            margin-bottom: 0.25rem;
        }

        .details-item p {
            font-size: 1.1rem;
            font-weight: 500;
        }

        /* --- RSVP Form --- */
        form {
            display: flex;
            flex-direction: column;
            gap: 1.2rem;
            text-align: left;
        }

        label {
            font-size: 0.85rem;
            font-weight: 600;
            color: var(--text-color);
        }

        input, select, textarea {
            width: 100%;
            padding: 0.8rem;
            border: 1px solid var(--pastel-pink-accent);
            border-radius: 8px;
            font-family: inherit;
            background-color: var(--pastel-pink-light);
            color: var(--text-color);
            outline: none;
        }

        input:focus, select:focus, textarea:focus {
            border-color: var(--pastel-rose);
            background-color: var(--white);
        }

        button {
            background-color: var(--pastel-pink-accent);
            color: var(--white);
            border: none;
            padding: 1rem;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.3s ease;
            margin-top: 0.5rem;
        }

        button:hover {
            background-color: var(--pastel-rose);
        }

        /* --- Footer --- */
        footer {
            padding: 2rem;
            font-size: 0.85rem;
            color: #8C7A7E;
        }

        /* --- Responsive Design --- */
        @media (max-width: 480px) {
            h1.name { font-size: 3rem; }
            .countdown-container { gap: 0.5rem; }
            .time-box { min-width: 60px; padding: 0.5rem; }
            .time-box span { font-size: 1.2rem; }
        }
    </style>
</head>
<body>

    <!-- Hero Header -->
    <header>
        <p class="subtitle">You are cordially invited to celebrate</p>
        <h1 class="name">Solana Keene</h1>
        <p class="subtitle">M. Carlos</p>
        <p class="event-tag">1st Birthday & Holy Baptism</p>

        <!-- Countdown -->
        <div class="countdown-container">
            <div class="time-box"><span id="days">00</span><label>Days</label></div>
            <div class="time-box"><span id="hours">00</span><label>Hours</label></div>
            <div class="time-box"><span id="minutes">00</span><label>Mins</label></div>
            <div class="time-box"><span id="seconds">00</span><label>Secs</label></div>
        </div>
    </header>

    <!-- Main Content -->
    <main>
        <!-- Event Details Card -->
        <section>
            <div class="card">
                <h2>Event Details</h2>
                <hr style="border: none; border-top: 1px solid var(--pastel-pink-main); width: 50%; margin: 0 auto 1.5rem auto;">

                <div class="details-item">
                    <strong>Date & Time</strong>
                    <p>Wednesday, November 11, 2026</p>
                    <p style="font-size: 0.95rem; font-weight: normal; color: #666;">Time: To be announced</p>
                </div>

                <div class="details-item">
                    <strong>Venue</strong>
                    <p>Festival Mall, Alabang</p>
                    <p style="font-size: 0.95rem; font-weight: normal; color: #666;">Muntinlupa City, Metro Manila</p>
                </div>

                <div class="details-item">
                    <strong>Attire Motif</strong>
                    <p>Shades of Pastel Pink & White</p>
                </div>
            </div>
        </section>

        <!-- RSVP Form Section -->
        <section>
            <div class="card">
                <h2>R.S.V.P.</h2>
                <p style="font-size: 0.9rem; margin-bottom: 1.5rem;">Please let us know if you can make it to our little princess's special day!</p>
                
                <form id="rsvp-form">
                    <div>
                        <label for="name">Your Name</label>
                        <input type="text" id="name" required placeholder="Enter your full name">
                    </div>

                    <div>
                        <label for="attendance">Will you attend?</label>
                        <select id="attendance" required>
                            <option value="" disabled selected>Select an option</option>
                            <option value="yes">Joyfully Accepts</option>
                            <option value="no">Regretfully Declines</option>
                        </select>
                    </div>

                    <div>
                        <label for="guests">Number of Guests</label>
                        <input type="number" id="guests" min="1" max="5" value="1">
                    </div>

                    <div>
                        <label for="message">Message for Solana</label>
                        <textarea id="message" rows="3" placeholder="Leave a sweet note for the birthday celebrant..."></textarea>
                    </div>

                    <button type="submit">Send RSVP</button>
                </form>
            </div>
        </section>
    </main>

    <!-- Footer -->
    <footer>
        <p>We can't wait to celebrate with you!</p>
        <p style="margin-top: 0.5rem; font-size: 0.75rem;">© 2026 Solana Keene M. Carlos</p>
    </footer>

    <!-- Countdown & Form Script -->
    <script>
        // Set the Target Event Date
        const eventDate = new Date("November 11, 2026 00:00:00").getTime();

        const updateTimer = () => {
            const now = new Date().getTime();
            const difference = eventDate - now;

            if (difference > 0) {
                const days = Math.floor(difference / (1000 * 60 * 60 * 24));
                const hours = Math.floor((difference % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
                const minutes = Math.floor((difference % (1000 * 60 * 60)) / (1000 * 60));
                const seconds = Math.floor((difference % (1000 * 60)) / 1000);

                document.getElementById('days').innerText = days.toString().padStart(2, '0');
                document.getElementById('hours').innerText = hours.toString().padStart(2, '0');
                document.getElementById('minutes').innerText = minutes.toString().padStart(2, '0');
                document.getElementById('seconds').innerText = seconds.toString().padStart(2, '0');
            } else {
                document.querySelector('.countdown-container').innerHTML = "<p>The Special Day is Here!</p>";
            }
        };

        setInterval(updateTimer, 1000);
        updateTimer();

        // Simple Form Submission Alert
        document.getElementById('rsvp-form').addEventListener('submit', function(e) {
            e.preventDefault();
            alert("Thank you for your RSVP! We have received your response.");
            this.reset();
        });
    </script>
</body>
</html>
